import type { MetadataRoute } from "next";
import { articles, articleUrl, categories, siteUrl, symptoms } from "@/lib/site-data";
export default function sitemap(): MetadataRoute.Sitemap { const updated = new Date("2026-08-24"); return [{ url: siteUrl, lastModified: updated, changeFrequency: "weekly", priority: 1 }, { url: `${siteUrl}/find-a-problem/`, lastModified: updated, changeFrequency: "monthly", priority: .8 }, ...categories.map((category) => ({ url: `${siteUrl}/${category.slug}/`, lastModified: updated, changeFrequency: "weekly" as const, priority: .8 })), ...symptoms.map((symptom) => ({ url: `${siteUrl}/symptoms/${symptom}/`, lastModified: updated, changeFrequency: "weekly" as const, priority: .7 })), ...articles.map((article) => ({ url: `${siteUrl}${articleUrl(article)}`, lastModified: new Date(article.updated_date), changeFrequency: "monthly" as const, priority: .8 })), ...["about", "editorial-policy", "contact", "privacy", "terms"].map((page) => ({ url: `${siteUrl}/${page}/`, lastModified: updated, changeFrequency: "yearly" as const, priority: .3 }))]; }
